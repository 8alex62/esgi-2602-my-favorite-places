services:
  db:
    image: postgres
    restart: unless-stopped
    networks:
      - net
    volumes:
       - db-data:/var/lib/postgresql
    environment:
      POSTGRES_PASSWORD: supersecretpassword

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - 8080:8080
    networks:
      - net

  api:
    image: ghcr.io/8alex62/esgi-2602-my-favorite-places:latest
    pull_policy: always
    networks:
      - net
    depends_on:
      - db
    restart: unless-stopped
    ports:
      - 3000:3000

networks:
  net:
    driver: bridge

volumes:
  db-data: